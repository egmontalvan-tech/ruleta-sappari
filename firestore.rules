rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /participaciones/{docId} {
      allow create, update: if true;
      allow read, delete: if false;
    }
  }
}
